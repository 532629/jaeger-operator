apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jaeger-allinone.fullname" . }}-test-connection"
  labels:
{{ include "jaeger-allinone.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "jaeger-allinone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
